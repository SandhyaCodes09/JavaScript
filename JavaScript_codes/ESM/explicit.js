// manually change the datatype
let a = '1234';
console.log(Number(a));

let b = 1234;
console.log(String(b));

let c = 0;
console.log(Boolean(c));

// ex:- 2

const user = "Rahul Mehta";
const age = 20;
const isSubscribed = true;
const scores = null;

// Use Number() to convert a string to a number
console.log(Number(user));
// Use Number() to convert a boolean to a number
console.log(Number(isSubscribed));
// Use String() to convert a number to a string
console.log(String(age));
// Use String() to convert a boolean to a string
console.log(String(isSubscribed));
// Use Boolean() to convert a string to a boolean
console.log(Boolean(user));
// Use Boolean() to convert a null to a boolean
console.log(Boolean(scores));

